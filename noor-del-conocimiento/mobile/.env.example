# Server-side only — set via EAS Secrets / hosting env, NEVER shipped in the client bundle.
ANTHROPIC_API_KEY=
# Comma-separated allowed origins for the feedback endpoint CORS policy.
FEEDBACK_ALLOWED_ORIGINS=https://noor-del-conocimiento.example.com
